# broker (Event Gate)

Risk-gate and briefing dashboard for futures paper trading. Package name is broker. Product/UI name is Event Gate. Paper BUY/SELL is MockBroker only (delayed last + hard stop). No live/demo Tradovate orders. No EnterLong on a live broker.

Trader: Richard. Timezone America/New_York.

## What it does
1s gate tick when enabled: idle, PRE-ARM (T-15 to T-2), NO-STOP BAND (T-2 to T+2), SESSION FLATTEN (flatten ET +/- 5 min). Cancels Market/StopMarket/StopLimit/MIT on gated roots in those windows. Cancels qty over 1. Limits left alone unless oversize. Flatten gated names at session cap or daily loss ($500). FOMC in the type forces 15:30 flatten. Gated roots: MES MNQ ES NQ ZN ZF ZT ZB SR3 6E M6E. MockBroker default. TradovateDemoBroker stub pinned to demo.tradovateapi.com/v1; construction throws when the URL contains live.
## Seed events
2026-09-04T12:30:00Z NFP flatten 15:45 ET. 2026-09-11T12:30:00Z CPI flatten 15:45 ET. 2026-09-16T18:00:00Z FOMC_STATEMENT flatten 15:30 ET. 2026-09-16T18:30:00Z FOMC_PC flatten 15:30 ET. Also inserted by db/migrations/001_init.sql.
## Design docs
- [System design](docs/DESIGN.md) — architecture and sleeve methodologies
- [Abbreviations](docs/ABBREVIATIONS.md) — tickers, futures roots, and product terms
- [iOS Event Gate](ios/README.md) — native phone essentials + FCM (users-table login)

## Layout
client/ Vite+React+TS SPA. Phones (narrow viewport or `/m`) get a compact essentials dashboard (gate, risk, autopilot chips, flatten, sleeve P/L); the desktop layout is unchanged. ios/ SwiftUI Event Gate (`com.logikmancer.mybroker`) is the native phone client (same essentials + FCM); open `ios/EventGate.xcodeproj` on a Mac. server/ Node 20 Express. shared/ clock types. db/migrations/ Postgres. deploy/ nginx systemd setup.sh deploy.sh. tests/ unit tests. No Docker.
## Massive market data
MASSIVE_API_KEY on Mac `.env` and VPS `/opt/broker/.env` (gitignored). Starter plan is 15-minute delayed stocks. Equities last quotes, S&P scan dailies, and the SPY/ACWI/HYG/UUP risk gate use `https://api.massive.com`. Futures (`=F` / MES,ZN,6E,M6E,SR3,ES,NQ,MNQ) still use Yahoo. App boots without the key (clear errors on equity Massive paths; Yahoo futures still work). Never commit the key.

## E*TRADE option chains
Option expiries and chains are live E*TRADE market data (`https://api.etrade.com/v1/`). Paper fills stay MockBroker. Never place E*TRADE orders. Event Gate renews the access token in-process every 30 minutes during the cash session (weekdays 9:30–16:00 America/New_York) via `GET {base}/oauth/renew_access_token` so the VPS does not need Richard's Mac. A 2-hour idle drop is what this prevents; midnight ET still needs a human PIN. Do that from the live dashboard (desktop header or phone essentials at `/m`): Authorize opens us.etrade.com, type the PIN into Event Gate. `POST /api/etrade/oauth/start` then `POST /api/etrade/oauth/pin` (requireAuth). Snapshot `etradeAuth` is `ok` | `needs_pin` | `error` with no token material. Emergency CLI remains `node scripts/etrade-oauth.mjs request|access|renew`. Optional `POST /api/etrade/renew` forces a keep-alive renew. Keys live in gitignored `.env.etrade` on the VPS (`ETRADE_ENV=production`, `ETRADE_PROD_KEY`/`SECRET`, `ETRADE_PROD_ACCESS_TOKEN`/`SECRET`). Never paste keys or put tokens on an agent box.

## Local development
Requires Node 20. Copy .env.example to .env. Install with npm, then: npm test ; npm run typecheck:server ; npm run typecheck:client ; npm run build:client ; npm run build:server. Dev API: npm run dev:server (127.0.0.1:3001). Dev UI: npm run dev:client (Vite 5173, proxies /api and /ws). If postgres/redis are missing, unit tests still pass; the server in development falls back to seed events and memory sessions. Production refuses to start without postgres, redis, and a cookie-signing secret (`SESSION_SECRET` or `GATE_PASSWORD` fallback) when `AUTH_MODE=users`. Create the first user with `BOOTSTRAP_ADMIN_USER` / `BOOTSTRAP_ADMIN_PASSWORD` (empty `users` table only) or `npm run user:create -- <username>`. Migrate with: npm run migrate.
## VPS deploy
No Docker. nginx + postgres + redis already on the box. Default path /opt/broker (DEPLOY_PATH override). First time: deploy/setup.sh (creates db/user, redis check, copies nginx and systemd, prompts for GATE_PASSWORD, build, migrate, enable event-gate.service). Later: deploy/deploy.sh (git pull unless dirty/no remote, npm ci, builds, migrate, restart unit, nginx test+reload). TLS comments live in deploy/nginx/event-gate.conf. server_name is event-gate.local or _.

iOS: native client is `ios/` (see ios/README.md) — essentials home + FCM. Server stays fail-closed until `PUSH_FCM_ENABLED=1` plus `PUSH_FCM_PROJECT_ID` and ADC or a root-owned service-account file on the VPS. Copy `~/Downloads/GoogleService-Info.plist` into `ios/EventGate/` before building; never commit that plist, Firebase JSON, user passwords, or FCM tokens. Token register/revoke and `POST /api/notifications/test` stay behind users-table auth. Simulator will not get real APNs.
## Security
Bind the API to localhost (BIND=127.0.0.1). Put nginx in front (TLS only; no htpasswd on /api or the SPA). Production login is the `users` table (argon2id) plus cookie `eg.sid` and/or bearer. GATE_PASSWORD is cookie-mode / signing only — not GATE ON/OFF. TRADING_MODE=live is refused. Tradovate stub throws on live hosts. Paper orders stay on MockBroker. No directional orders to Tradovate.
## NinjaTrader API key
A funded live NT account (at least one thousand USD) plus the about twenty-five USD/month API add-on is not required for mock. Do not buy it for paper. This repo is briefing plus risk-gate, not an NT order router.
## Tests
npm test runs vitest. No live postgres required. clock.test.ts injects Date. gate.test.ts covers cancel/flatten on MockBroker. tradovate.test.ts refuses live URLs.
## API on 127.0.0.1:3001
GET /api/status. GET /api/quotes?sleeve=day|momentum|options|ownership|riskoff (Massive Starter equities 15m delayed, Yahoo futures =F; 45s cache; also marks mock stops). GET /api/scan?sleeve=momentum|ownership (S&P 500 delayed daily scan via Massive aggs, requireAuth; status scanning|ok; Paper this prefills MockBroker, does not auto-place). POST /api/paper/order and POST /api/paper/close (MockBroker paper, requireAuth). GET /api/options/expiries?symbol=SPY and GET /api/options/chain?symbol=AAPL&expiry=YYYY-MM-DD (live E*TRADE chains; any ordinary US ticker; paper fills stay mock). POST /api/etrade/oauth/start (requireAuth) starts the production PIN handshake and returns `{ ok, authorizeUrl }` for the client to open (server does not log the URL). POST /api/etrade/oauth/pin `{ pin }` exchanges the verifier, upserts `.env.etrade` mode 600, updates `process.env`, returns `{ ok: true }` with no secrets. POST /api/etrade/renew (requireAuth) forces an in-process E*TRADE access-token renew (no secrets in the body). Snapshot includes `etradeAuth` (`ok` | `needs_pin` | `error`). POST /api/paper/vertical opens a two-leg debit vertical on the options sleeve (call or put) or the riskoff sleeve (puts only) (MockBroker, never an E*TRADE/Tradovate order; no new verticals after 15:50 ET weekdays / weekends closed; net debit cannot exceed half the width; same-underlying cooldown the rest of the ET day after a 50% debit stop). POST /api/paper/csp and POST /api/paper/covered-call are paper ownership overlay (options sleeve, tagged thesisSleeve=ownership|spcx; never naked; autopilot does not sell puts/calls). Snapshot sleeveBooks include totalPnlUsd (equity-100k) and dailyPnlUsd. POST /api/paper/auto toggles autopilot per sleeve. `{ enabled }` sets ALL five (old clients); `{ sleeveId, enabled }` sets one; `{ sleeves: { day, momentum, options, ownership, riskoff } }` batches. Snapshot `autoPaperBySleeve` is the source of truth; `autoPaper` is true if ANY sleeve is on. Default all on when Redis has no key; legacy `paper:auto` `0`/`1` migrates to JSON on first boot (all off / all on). Mock longs + debit-call verticals when that sleeve AUTO is on and RISK ON; risk-off puts require equity 200dma break (SPY below) for SPY/QQQ/IWM, plus one HYG ATM put debit when credit is the broken leg (HYG below 200dma); plus one GLD/UUP/TLT/IEF/XLU/XLP vs BIL relative-strength ETF long on the riskoff sleeve when RISK OFF; never CSP/CC/naked from auto; day sleeve auto-papers MES 5m stochastic 14,3,3 vs VWAP qty 1 only when day AUTO is on, GATE windows still veto. Snapshot includes autoPaper, autoPaperBySleeve, riskOn/riskChecks (automated SPY/ACWI/HYG 200dma + UUP 20d veto; does not bind day/clock/flatten/GATE), and sleeveBooks ($100k mock equity per sleeve, including riskoff). POST /api/sleeves/:id/fills paper journal (not broker). DELETE /api/sleeves/:id/fills/:fillId. GET and PUT /api/freeze. POST /api/knowledge-time. POST /api/gate/enable. POST /api/flatten. POST /api/cancel-stops. POST /api/mock/inject-stop. GET /api/orders. GET /api/events. GET /api/log. WebSocket /ws for live status plus log. GET /api/health includes `notifications.{provider,enabled,configured}` only (no tokens or credential paths). GET /api/notifications/status (requireAuth) adds token counts. POST /api/notifications/tokens/register and /revoke (requireAuth) persist iOS FCM tokens. POST /api/notifications/test (requireAuth) sends a harmless test push when FCM is configured, otherwise `{ outcome: disabled|not_configured }`. Login: POST /api/auth/login with password, cookie eg.sid. No live EnterLong.
